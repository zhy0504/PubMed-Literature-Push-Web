# Docker环境配置文件
# 复制此文件为 .env 并修改相应的值

# ==================== 应用配置 ====================
SECRET_KEY=your-very-secret-key-here-change-this-in-production

# 调试模式（仅在使用 flask run 时生效，Docker/Gunicorn 中始终是生产模式）
# 0=关闭调试, 1=开启调试（仅用于本地开发，不要在生产环境开启）
FLASK_DEBUG=0

# ==================== 数据库配置 ====================
# 注意：Docker环境会自动使用 sqlite:////app/pubmed_app.db，无需配置
# DATABASE_URL 仅在非Docker环境（本地开发）时需要设置
# DATABASE_URL=sqlite:///pubmed_app.db

# ==================== PubMed API配置 ====================
# PubMed API密钥（可选，提高请求限制）
PUBMED_API_KEY=your-pubmed-api-key-here
# 每次检索最大条数（仅首次初始化时使用，之后在Web界面修改）
PUBMED_MAX_RESULTS=100
# 请求超时时间/秒（仅首次初始化时使用，之后在Web界面修改）
PUBMED_TIMEOUT=30

# ==================== OpenAI AI配置 ====================
# OpenAI API密钥（用于AI检索式生成和摘要翻译）
# 优先使用数据库配置，如果数据库中没有配置则使用此环境变量
OPENAI_API_KEY=your-openai-api-key-here
# OpenAI API基础地址（支持兼容接口，如DeepSeek、通义千问等）
OPENAI_API_BASE=https://api.openai.com/v1

# ==================== 日志配置 ====================
# 日志级别: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
# 日志文件路径（Docker 使用命名卷，自动轮转 10MB/文件，保留5个备份）
LOG_FILE=/app/logs/app.log

# ==================== 邮件配置（可选，已被Web界面邮件管理替代）====================
# 这些配置已被数据库中的邮件配置替代，仅作为后备值
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=1
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password